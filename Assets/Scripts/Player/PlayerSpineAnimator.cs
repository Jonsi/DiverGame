using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class PlayerSpineAnimator : MonoBehaviour
{
/*    public SkeletonAnimation SkeletonAnimation;
    public AnimationReferenceAsset IdleOnBoatAnim, JumpFromBoatAnim, IdleinWaterAnim, SwimAnim;
*/
/*    public void SetAnimation(AnimationReferenceAsset animation, PlayerSkin skin, bool loop = true)
    {
        SkeletonAnimation.state.SetAnimation(0, animation, loop);

        if (SkeletonAnimation.initialSkinName != skin.ToString())
        {
            SkeletonAnimation.initialSkinName = skin.ToString();
            SkeletonAnimation.Initialize(true);
        }
    }*/
}
